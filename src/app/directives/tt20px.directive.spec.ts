import { Tt20pxDirective } from './tt20px.directive';

describe('Tt20pxDirective', () => {
  it('should create an instance', () => {
    const directive = new Tt20pxDirective();
    expect(directive).toBeTruthy();
  });
});
